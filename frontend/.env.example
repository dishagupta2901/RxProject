# Base URL of the RxFlow API. Defaults to http://localhost:5080 (both plain `dotnet run`,
# see src/RxFlow.Api/Properties/launchSettings.json, and the Compose host mapping in
# deploy/docker-compose.yml use this port) if unset.
VITE_API_BASE_URL=http://localhost:5080
