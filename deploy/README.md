# Local deployment

Run `docker compose -f deploy/docker-compose.yml up -d` from the repository root. The stack uses synthetic credentials and isolated local ports only. API and frontend container definitions will be added after their build pipelines are verified.
